# Dependencies
node_modules/
package-lock.json
pnpm-lock.yaml

# Configuration & Secrets
config.json
config.json.bak
allowedUser.json
command-history.json
.env
.env.local

# Build & Output
dist/
*.js
!index.js

# Portable Node 26 runtime (OpenTUI requires node:ffi / Node >= 26)
node-runtime/*
!node-runtime/download.ps1
!node-runtime/download.sh

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/

# IDE & Editor
.vscode/
.idea/
*.swp
*.swo
*.sublime-*
*.code-workspace

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Backup Files
*.bak
*.backup
*~

# Old/Temporary Files
src/bot_old*
*_old.ts
temp/
tmp/
tsc_errors.txt
data/
project_archive.zip
diff.txt
answer.txt

# Dev-only harnesses (spikes, live tests, RCON tool)
src/spike-*.ts
src/spike-*.tsx
src/test-drive.ts
src/test-session.ts
src/test-mcp.ts
src/rcon-cli.mjs

# Stray accidental directory (recursive copy of a user home folder)
Users/

# Repo-local agent instructions (kept locally, not committed)
AGENTS.md

# Local AI tooling (opencode plugin + vendored deps)
.opencode/

# Generated knowledge graph (regenerate with `graphify update .`)
graphify-out/